import FAQClient from "./faq.client";

export const metadata = {
  title: "FAQ | Syntra Digital Services & Process",
  description: "Answers to common questions about Syntra Digital’s services, process, timelines, pricing ranges, and what to expect when working together."
};

export default function FAQPage() {
  return <FAQClient />;
}
